<!--pages/login/map/map.wxml-->
<view class='map'>
 <view class='map'>
 <map
      id="myMap"
      style="width 100%; height: 80%"
      latitude="{{latitude}}"
      longitude="{{longitude}}"
      markers="{{markers}}"
      covers="{{covers}}"
      show-location
    ></map>
    <view class="btn-area">
    <button bindtap="getCenterLocation" class="page-body-button" type="primary">导航</button>
  </view>
 </view>

</view>
